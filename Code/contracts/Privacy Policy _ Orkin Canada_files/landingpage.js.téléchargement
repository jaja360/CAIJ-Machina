//Set max height for landing page image 
jQuery(document).ready(function($) {
    if (window.matchMedia('(max-width: 1024px)').matches) {
        var $form = $('.one-third');
        var $heroBody = $('.masthead0325');
        var childOffset = $form.offset();
        var parentOffset = $heroBody.offset();
        var maxHeight = childOffset.top - 75;
        $heroBody.css('max-height', maxHeight + 'px');
        
        var title = $('.masthead0325 form ul li h3.title');
        //$('.gform_wrapper').css('min-height', titleheight + 'px');
    }
});

